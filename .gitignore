# ==============================================
# UE项目核心忽略规则（标准模板优化版）
# ==============================================
# Visual Studio / Rider / CLion 等IDE生成的文件
.vs/
.idea/
*.sln
*.suo
*.vcxproj
*.vcxproj.filters
*.xcodeproj
*.xcworkspace

# 编译产物（二进制/中间文件）
*.exe
*.dll
*.so
*.dylib
*.a
*.lib
*.pdb
*.ilk
*.obj
*.o
*.lo
*.slo
*.gch
*.pch

# UE引擎自动生成的项目/配置文件（非手动创建）
*.opensdf
*.sdf
*.VC.db
*.VC.opendb
.vsconfig
*.log
*.ini

# ==============================================
# UE项目构建/缓存/临时文件（强制忽略）
# ==============================================
# 编译中间文件
Intermediate/
Plugins/**/Intermediate/

# 编辑器缓存与数据
Saved/
DerivedDataCache/
ShaderCache/
*.ushaderbytecode
*.ushaderlibrary

# 构建输出文件
Build/
!*PakBlacklist*.txt  # 保留构建工具依赖的黑名单文件（如PakBlacklistDevelopment.txt）

# 地图编译产物
*_BuiltData.uasset
*_BuiltData.uexp
*_BuiltData.ubulk

# 预编译资源（避免重复管理）
SourceArt/
SourceArt/**/*.png
SourceArt/**/*.tga
SourceArt/**/*.bmp

# ==============================================
# 针对性忽略：仅保留 Content 和核心项目文件
# ==============================================
# 忽略C++源代码（若无需管理代码，保留则删除此行）
Source/

# 忽略项目配置（若无需同步引擎/项目设置，保留则删除此行）
Config/

# 忽略插件（若无需管理第三方/自定义插件，保留则删除此行）
Plugins/

# ==============================================
# 核心保留项（必须跟踪）
# ==============================================
# 根目录唯一的 .uproject 文件（UE项目入口，自动匹配）
!.uproject

# Content 文件夹及其所有内容（游戏资源核心目录）
!/Content/
!/Content/**


# ==============================================
# 补充：避免误忽略的特殊情况（可选）
# ==============================================
# 若需保留特定配置文件（如DefaultEngine.ini），取消注释以下两行：
# !Config/
# !Config/DefaultEngine.ini

# 若需保留特定插件（如MyPlugin），取消注释以下两行：
# !Plugins/
# !Plugins/MyPlugin/

# 若需保留Shader编译产物（罕见需求），取消注释：
# !Saved/Cooked/
# !Saved/Cooked/**/Shaders/
